DB_HOST=localhost
DB_NAME=cancercare
DB_USER=postgres
DB_PASSWORD=change_me
DB_PORT=5432
PORT=5000
# Optional in production; local development creates and stores a private key in backend/.auth-secret.
AUTH_SECRET=
